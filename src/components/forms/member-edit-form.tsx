import { EditMemberFormProps } from "@/typings/member-types";
import React from "react";

export default function MemberEditForm({
    isFormOpen,
    setFormOpen,
    memberData,
    refresh,
}: EditMemberFormProps) {
    return <div></div>;
}
